name: food_manager
description: "Eating planner app."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  logging: ^1.3.0
  provider: ^6.1.2
  shared_preferences: ^2.3.5
  mobile_scanner: ^6.0.2
  json_view: ^0.4.2
  openfoodfacts: ^3.18.1
  sqflite: ^2.4.1
  path: ^1.9.0
  fuzzy: <1.0.8
  collection: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true