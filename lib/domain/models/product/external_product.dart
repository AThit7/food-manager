class ExternalProduct {
  final String? barcode, name, referenceUnit;
  final double? containerSize, calories, carbs, protein, fat;

  const ExternalProduct({this.barcode, this.name, this.referenceUnit,
    this.containerSize, this.calories, this.carbs, this.protein, this.fat});
}