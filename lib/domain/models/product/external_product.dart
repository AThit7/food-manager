class ExternalProduct {
  final String? barcode, name, referenceUnit, tag;
  final double? containerSize, calories, carbs, protein, fat;

  const ExternalProduct({this.barcode, this.name, this.referenceUnit, this.tag, this.containerSize, this.calories,
    this.carbs, this.protein, this.fat});
}